Task: add agent-related repository files and commit only the relevant changes.

Done criteria:
- Track the existing AGENTS.md file.
- Track the existing CLAUDE.md file.
- Add a lowercase claude.md compatibility entrypoint.
- Create trackable .codex/ and .opencode/ directories.
- Keep unrelated untracked files out of the commit.
- Verify the new files exist and the staged set is scoped correctly.
- Create a git commit for the staged changes.

Tradeoffs:
- Preserve existing AGENTS.md and CLAUDE.md contents instead of rewriting them.
- Use .gitkeep placeholders in hidden config directories to avoid adding extra markdown files.
- Add minimal _CN counterparts for new markdown files to follow repository convention.
